const websocket = {
    send: ({
        data,
        connectionId,
    }: {
        data: {
            message?: string,
            type?: string,
            from?: string,

        },
        connectionId: string,
    }) => {

    }
}